
name: build
on: [pull_request, push]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      # 1. 拉取代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 强制使用官方 Gradle 源（保留版本号）
      - name: Force official Gradle distribution
        run: |
          sed -i 's#https://[^/]*/gradle/#https://services.gradle.org/distributions/#g' gradle/wrapper/gradle-wrapper.properties

      # 3. 安装 Java
      - name: Setup JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: 25
          distribution: 'temurin'

      # 4. 配置 Gradle（自动缓存，替代手动 actions/cache）
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      # 5. 赋予执行权限
      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      # 6. 构建
      - name: Build
        run: ./gradlew build --no-daemon

      # 7. 上传构建产物
      - name: Upload artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Artifacts
          path: build/libs/